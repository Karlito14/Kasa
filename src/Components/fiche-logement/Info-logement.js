import React from "react";

const Info = (location) => {
    const title = location.title;
    const localisation = location.location;
    const tags = location.tags;
    const etoiles = location.rating;
    const hote = location.host
};

export default Info;